# Copy to .env and fill in real values. NEVER commit .env (it is gitignored).

# Brave Search API — financial news for sentiment analysis
BRAVE_API_KEY=

# Claude API — CISC 699 migration target for agents/strategy_agent/llm_client.py
# (uncomment and fill when the migration begins)
# ANTHROPIC_API_KEY=

# LLM backend for the decision engine: claude (default) or ollama
LLM_BACKEND=claude
